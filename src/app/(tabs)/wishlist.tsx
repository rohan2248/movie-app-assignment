import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function WishlistScreen() {
  return (
    <PlaceholderScreen
      icon="wishlist"
      title="Wishlist"
      body="Saved movies, persisted on the server and mirrored on-device, arrive in Phase 7."
    />
  );
}
